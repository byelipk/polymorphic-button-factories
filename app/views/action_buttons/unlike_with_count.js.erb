var list        = document.querySelector(".btn-list");
var oldBtnGroup = document.querySelector(".btn-group__like");
var fragment    = document.createRange().createContextualFragment("<%= j like_with_count_button %>");
var newBtnGroup = fragment.querySelector(".btn-group");

alert("You crushed someone's hopes and dreams!");

list.replaceChild(newBtnGroup, oldBtnGroup);